<!DOCTYPE html>
<html>
  <head>
    <title>Shop</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= render "shared/navbar"%>
    <% if notice.present? %>
      <div class="absolute block px-4 py-3 text-green-700 bg-green-100 border border-green-400 rounded right-20 w-fit">
        <p class=""><%= notice %></p>
      </div>
    <% end %>
    <% if alert.present? %>
      <div class="absolute block px-4 py-3 text-red-700 bg-red-100 border border-red-400 rounded right-20 w-fit">
        <p class=""><%= alert %></p>
      </div>
    <% end %>
    <main class="container flex px-5 mx-auto mt-28">
      <%= yield %>  
    </main>
  </body>
</html>
