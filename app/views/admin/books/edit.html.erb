<div class="w-full mx-auto md:w-2/3">
  <%= form_with(model: @book, url: admin_book_path(@book), method: :patch, data: { turbo: false }) do |form| %>
      <h1 class="text-4xl font-bold">編輯書籍</h1>

      <%= render "form", form: form %>
  <% end %>
</div>
