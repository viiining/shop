<div class="w-full mx-auto md:w-2/3">
  <%= form_with(model: @coupon, url: admin_coupon_path(@coupon), method: :patch, data: { turbo: false }) do |form| %>
      <h1 class="text-4xl font-bold">編輯書籍</h1>

      <%= render "form", form: form %>
  <% end %>
</div>
